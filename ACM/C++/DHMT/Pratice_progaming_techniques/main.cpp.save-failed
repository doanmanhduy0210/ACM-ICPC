#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
const int maxn = 3101010;

#define pts() ios_base::sync_with_stdio(NULL),cin.tie(0),cout.tie(0);

#define w first
#define val second

int a[5];
int main(int argc, char* argv[])
{
    //freopen("in.txt","r",stdin);
    cin >> a[0] >> a[1] >> a[2] >> a[3];
    sort(a,a+4);
    if(a[0]+a[3]==a[1]+a[2] ||
       a[0]+ a[1] == a[2]+a[3] ||
       a[0] + a[2] == a[1]+a[3]||) puts("YES");
        else puts("NO");

    return 0;
}
